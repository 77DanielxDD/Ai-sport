# AI Sport 项目指南

> 面向项目交接、技术面试与演示答辩的说明文档。
>
> 本文只描述仓库当前可确认的实现和已记录的评测结果。对尚未做真实数据验证的能力，会明确标记为边界或后续方向，避免把“代码具备”写成“线上指标已证明”。

## 1. 项目定位

AI Sport 是一个面向个人健身场景的“视频动作分析 + Agentic RAG 智能问答”系统。用户上传训练视频后，系统异步执行姿态检测与动作评分；用户还可以针对动作纠正、训练计划、报告解读、训练趋势和通用健身知识提出问题。

项目的核心不是让模型自由聊天，而是让问题进入一条可控的业务链路：先识别问题类型，再选择必要工具，检索或读取真实数据，最后返回带结构化字段和知识引用的答案。没有知识证据或用户训练数据时，系统应明确说明数据不足，而不是补造结论。

## 2. 架构总览

```text
React + Vite
     |
     | HTTP / SSE
     v
Spring Boot (Java)
  |- JWT 鉴权、用户和管理 API
  |- 视频上传、MySQL 持久化
  |- RabbitMQ 生产/消费、任务状态机
  |- /api/agent/qa 聚合入口
  |- PythonAgentClient：优先调用 Python Agent，失败时 Java 侧降级
     |
     | HTTP/1.1 JSON
     v
FastAPI (Python AI Service)
  |- MediaPipe + OpenCV 视频动作分析
  |- LangChain Agent 问答编排
  |- Chroma + BM25 Hybrid RAG
  |- SiliconFlow BGE Reranker API
  |- GLM-4-Flash 结构化回答生成
     |
     +------------------ Redis：问答、检索、报告、用户记忆缓存
     +------------------ MySQL：用户、视频、任务、分析结果
     +------------------ RabbitMQ：视频分析异步任务
```

### 为什么采用 Java + Python 双端？

Java 和 Python 分别承担擅长的职责。Spring Boot 更适合承载鉴权、事务、数据持久化、消息队列和管理接口等长期运行的业务后端；Python 生态对 MediaPipe、OpenCV、sentence-transformers、LangChain 和 Chroma 更成熟。把视觉分析和 RAG/Agent 放在 Python 服务中，可以减少 JVM 中集成 AI 依赖的复杂度，也便于独立升级模型或检索策略。

双端的代价是网络调用、协议一致性和失败处理。项目因此使用固定 JSON Schema，Java 客户端显式指定 UTF-8 与 HTTP/1.1，并在 Python 服务不可用、超时或返回非 200 时进入 Java 降级路径。这样做的目标是“业务入口不断”，而不是假设 AI 服务永远可用。

## 3. 两条核心业务链路

### 3.1 视频分析链路

1. 前端上传视频到 Java 后端。
2. 后端创建 `AnalysisTask`，状态初始为 `QUEUED`，并将任务发送到 RabbitMQ。
3. 消费者通过 `taskId` 调用 `markProcessingIfQueued()`：只有 `QUEUED -> PROCESSING` 的首次原子迁移可以开始分析。
4. Python AI 服务使用 MediaPipe/OpenCV 提取人体关键点、角度、动作次数、节奏、对称性和关键帧等结果。
5. Java 后端写入分析结果并将任务迁移到 `COMPLETED`；异常情况下进入 `FAILED`，用户可发起新的分析尝试。
6. `TaskEventBroadcaster` 通过 SSE 推送状态变化；前端连接失败时退回轮询，并按 `1s -> 2s -> 4s -> 8s` 退避。

### 为什么任务幂等键选择 taskId，而不是 videoId？

同一个视频允许再次分析，例如用户修正拍摄角度后重试。因此 `videoId` 不能表达一次具体执行。`taskId` 表示一次独立尝试，是消息重复投递时的唯一真相：同一条 RabbitMQ 消息即使重投多次，也只能由第一次 `QUEUED -> PROCESSING` 迁移启动分析；新的重试会创建新的 `taskId`，从而允许再次执行。

### 3.2 Agent 问答链路

1. 前端调用 Java 的 `/api/agent/qa`。
2. Java 从认证上下文构造 `user_id`、`username`、`question`、可选 `focus_video_id`，请求 Python `/agent/chat`。
3. Python Agent 对问题进行意图识别，并根据意图生成工具计划。
4. Agent 执行一个或多个工具：知识检索、视频报告、训练历史、评分趋势、用户记忆。
5. 结果进入结构化回答生成，输出摘要、诊断、建议、训练计划、工具调用记录和知识引用。
6. 对缺少知识证据或缺少训练数据的情形，返回数据不足或无证据提示；对 Python 服务异常，Java 侧可使用本地降级编排。

### 为什么不让 LLM 直接根据用户问题回答？

健身建议涉及动作安全和个人训练状态。直接生成会有两个问题：一是回答无法追溯到知识来源，二是模型可能假设用户存在某些训练记录。工具编排将“用户历史”“视频报告”“健身知识”变为显式数据依赖，回答中保留 `tool_calls` 和 `references`，便于前端展示、日志审计和问题排查。

## 4. Agent 设计

### 4.1 四类意图和五类工具

| 意图 | 典型问题 | 主要工具 | 为什么这样规划 |
|---|---|---|---|
| 动作纠正 | 深蹲膝盖内扣怎么办 | `search_knowledge`，可选 `get_video_report` | 需要通用动作证据；带视频 ID 时再使用个人报告 |
| 训练计划 | 每周三练怎么安排 | `get_training_history`、`get_user_memory`、`search_knowledge` | 计划既要遵循训练原则，也要考虑用户历史 |
| 趋势回顾 | 最近是进步还是下降 | `get_score_trend`、`get_training_history` | 结论必须来自历史数据，不能只由知识库生成 |
| 通用知识 | 组间休息多久 | `search_knowledge` | 主要依赖知识库，避免凭空回答 |

五个工具分别为：`search_knowledge`、`get_video_report`、`get_training_history`、`get_score_trend`、`get_user_memory`。

### 4.2 结构化输出

Python 端用 Pydantic 模型约束回答字段：

```text
intent
summary
fallback
diagnosis[]
recommendations[]
training_plan[]
references[]
tool_calls[]
```

这样设计不是为了“让 JSON 看起来规范”，而是为了把稳定业务字段从自然语言中抽离。前端能够独立渲染问题诊断、建议优先级和知识引用；测试能够检查工具是否被调用；降级回答也可保持同一协议。

### 4.3 缓存策略

Redis 缓存的对象包括 Agent 问答结果、RAG 检索结果、视频报告和用户记忆。问答缓存 key 包含用户、可选视频 ID 和问题摘要；问答默认 TTL 为 180 秒。

缓存的目的不是替代数据库，而是降低短时间内重复提问、连续刷新页面、重复读取报告造成的工具调用和模型成本。缓存失效后仍以后端数据源为准。当前的 MD5 Hash 用于问题缓存 key，不是文档增量索引 Hash。

## 5. RAG 检索链路

### 5.1 索引流程

知识文件由 `ingest.py` 读取，清洗多余空白后按段落和句子切分，生成稳定 `chunk_id`。当前导入的知识文档被切分为 `24` 个 chunk；每个 chunk 同时写入 Chroma 和 BM25。

```text
知识文档
  -> 文本清洗
  -> 分块（chunk）
  -> chunk_id / metadata
  -> Chroma 向量索引
  -> BM25 倒排索引
```

当前 `/rag/reindex` 是**全量重建**：先清空 Chroma 和 BM25，再对完整知识文档重新切分和建立索引。对当前单文档、轻量项目来说，这种方式更简单、更不易出现脏数据或删除漏同步问题；当文档规模增大、重建耗时不可接受时，再演进到按文档 Hash、版本号和 chunk diff 做增量 upsert/delete。

### 5.2 Hybrid Retrieval 原理

`hybrid_retrieve()` 对同一问题并行获取：

- Chroma 稠密检索：通过 embedding 的语义相近性召回表达不同但语义相近的内容。
- BM25 稀疏检索：通过词项匹配强调动作名称、专业术语和关键错误描述。

系统再以 `chunk_id` 合并两路结果，并按加权分数排序：

```text
final_score = dense_score * 0.7 + normalized_bm25_score * 0.3
```

当前稠密分数使用结果 rank 的归一化近似，而不是直接暴露向量距离；BM25 分数按候选集合最大值归一化。这样可以将两个量纲不同的信号融合，但它不是严格的概率模型，因此权重需要通过离线评测而非主观感觉调整。

### 5.3 Reranker

知识工具会先取 `top_k * 3` 个候选，再调用 SiliconFlow 的 `BAAI/bge-reranker-v2-m3` API 精排，最后截取 top-k。Reranker 比双塔 embedding 更适合做精细相关性判断，因为它可以同时阅读 query 与候选文本。

外部 Reranker API 没有 Key、超时或返回异常时，系统回退到关键词启发式排序。这样做优先保证问答链路可用，而不是让一次外部依赖故障中断检索。面试时应说明：当前已接入远程 Reranker 调用和 fallback，但若要比较“API 精排相对启发式重排的收益”，仍需分别记录命中模式并运行对照评测。

### 5.4 为什么 Query Rewriting 没保留在主链路？

当前仓库曾保留 `rewrite_query()` 函数，但它没有被 `search_knowledge()` 调用，因此不属于实际运行主链路。它与 `pytrec_eval` 无关：评测框架只负责测量检索排序，不会阻止查询改写。

未默认启用改写的原因是轻量项目需要先保证可解释和可复现：改写会改变原始 query，可能让离线 qrels 的问题与实际检索输入不一致，还会增加一次模型调用、延迟和故障点。若后续接入，应记录 `original_query`、`rewritten_query`、改写模型版本，并分别评测改写前后的检索指标。

## 6. 评测体系

### 6.1 为什么不使用 RAGAS/LLM 裁判评价检索？

RAG 检索的目标是“相关 chunk 是否被排到前面”，这是一个标准信息检索问题。项目使用 `pytrec_eval` 构造 qrels/run：

- qrels：人工指定每个问题对应的相关 `chunk_id` 与相关性等级。
- run：检索器对每个问题返回的 chunk 排名和分数。

这套机制的优点是稳定、可重复、成本低。LLM 裁判适合辅助评估开放式生成质量，但它会受模型版本、Prompt、温度和评委偏好影响，不适合做高频检索回归的唯一标准。

### 6.2 RAG 评测指标

当前固定 RAG 评测集有 `8` 条与真实 `chunk_id` 对齐的问题，支持：

| 指标 | 回答面试官时的解释 |
|---|---|
| MAP | 多个相关文档时，整体排序质量的平均值 |
| NDCG@10 | 前 10 个结果中，高相关内容是否更靠前 |
| Recall@K | 前 K 个候选是否覆盖了应找到的相关 chunk |
| Precision@K | 前 K 个结果中有多少真正相关，体现噪声比例 |
| Reciprocal Rank | 第一个正确结果出现得是否足够靠前，可视为单相关答案场景下的 MRR 表达 |

历史容器测试曾记录 MAP 约 0.49、NDCG@10 约 0.62、Recall@5/10 为 1.0；知识文档替换后应重新运行 `pytrec_eval` 生成当前版本的正式报告，不能把历史值直接当成新知识文档的指标。

### 6.3 Agent 规划评测

Agent 固定集有 `20` 条样本：动作纠正 6 条、训练计划 4 条、趋势回顾 4 条、通用知识 6 条，其中 16 条要求知识 grounding。

离线评测在禁用 LLM 路由的条件下验证规则意图和工具计划，记录结果为：

```text
intent_accuracy = 1.0
tool_plan_exact_match = 1.0
tool_selection_precision = 1.0
tool_selection_recall = 1.0
tool_selection_f1 = 1.0
grounding_plan_coverage = 1.0
```

这代表固定集上的“意图与工具规划正确”，不代表 LLM 生成内容的事实正确率为 100%。面试时主动说明这个边界，反而能体现评测方法严谨。

### 6.4 真实 API 回归

在知识库构建成功后，真实链路 `Java /api/agent/qa -> Python /agent/chat -> Tool/RAG` 运行 20 条请求的记录为：

```text
HTTP/API 成功：20/20
P50 延迟：868ms
P95 延迟：1002ms
知识相关问题返回引用：15/15
```

剩余 5 条趋势回顾请求依赖用户训练历史和趋势数据；测试账号没有可用训练数据，因此按规则返回“暂时无法获取所需训练数据”。这验证的是失败时不编造，而不是趋势问答正确率。

## 7. 可靠性与工程实践

### 7.1 状态机与幂等

任务状态包括 `QUEUED`、`PROCESSING`、`COMPLETED`、`FAILED`、`CANCELLED`。重复消息问题由 `markProcessingIfQueued(taskId)` 处理：读取到非 `QUEUED` 状态就返回 false，不再启动 AI 分析；终态再次写入也会计入 duplicate skip。

这比“消费者收到消息就直接调用 Python”更可靠，因为 RabbitMQ 的至少一次投递语义意味着重复消息在网络故障、确认失败和人工重发中是正常现象。

### 7.2 SSE 与轮询的组合

SSE 在任务状态变化时减少轮询；但浏览器网络切换、代理超时和 EventSource 限制都会造成连接断开。因此前端不能只依赖 SSE，断线后需要退回带退避的轮询。组合策略同时追求更低读压力和更强可恢复性。

### 7.3 可观测性

Micrometer 指标包含：

```text
ai_sport_task_duplicate_skip_total
ai_sport_task_status_transition_total
ai_sport_task_poll_request_total
```

任务服务还能汇总 sample size、完成/失败/处理中/取消数量、平均端到端耗时、P95 端到端耗时与完成率。当前已完成核心状态机单测和指标埋点；真实视频全链路的重复分析次数、SSE 送达率和 P95 耗时仍需在启动 MySQL/RabbitMQ/AI 服务后使用 smoke 脚本形成正式报告。

## 8. 面试官高频深挖问答

### Q1：为什么不把 RAG 放在 Java 后端，反而维护 Java 和 Python 两个服务？

**答：** Java 是业务边界和稳定 API 的主入口，Python 是 AI 能力服务。视频姿态、embedding、向量库、LangChain 都在 Python 生态更成熟，强行全部迁入 Java 会降低开发效率并增加依赖复杂度。双端的代价通过明确 API 协议、超时、UTF-8、HTTP/1.1 和降级路径控制。这个拆分不是为了“微服务化而微服务化”，而是按能力边界拆分。

### Q2：为什么 Chroma 和 BM25 要同时使用？单独向量检索不够吗？

**答：** 向量检索擅长语义泛化，但对“深蹲、膝盖内扣、45 度”等精确术语未必稳定；BM25 对词面命中敏感，适合动作名和错误名。混合召回先降低漏召回，再让 Reranker 做精排。只有当 qrels 评测显示混合检索优于单路时，这个复杂度才值得保留。

### Q3：现有 BM25 对中文是否有局限？

**答：** 有。当前实现使用空白分词，中文连续文本的词法边界不理想，BM25 的收益可能被削弱。轻量项目可以先依靠 dense retrieval 和 Reranker 保持可用；要提升中文稀疏检索，应接入 jieba、pkuseg 或领域词典，并用同一 qrels 比较改造前后的 MAP/NDCG/Recall，而不是只凭主观回答质量判断。

### Q4：为什么 Reranker 放在召回后，而不是直接给所有知识块打分？

**答：** Cross-encoder 精排更准确但计算/网络成本更高，直接对全库做会让请求延迟随文档规模线性增长。先用 Chroma/BM25 把候选缩小到 `top_k * 3`，再精排是典型的“召回优先、排序精细”的两阶段检索结构。

### Q5：为什么 RAG 评测不用 RAGAS？

**答：** 当前目标是验证检索排序，最可控的对象是 qrels/run。pytrec_eval 不依赖评委模型，结果稳定、成本低，适合回归。RAGAS 可以作为生成质量的辅助实验，但不能替代检索标准指标，更不能把 LLM 主观打分当成唯一验收。

### Q6：Agent 评测 100% 是否意味着系统问答准确率 100%？

**答：** 不意味着。100% 指固定测试集上意图识别和工具计划与人工期望完全一致。最终答案质量还依赖检索命中、用户数据完整性、Reranker、LLM 生成和引用约束。项目分别用 RAG 检索指标、Agent 规划指标和真实 API 成功/延迟指标描述不同层级，避免一个数字覆盖全部质量。

### Q7：为什么趋势问题没有训练数据时不让模型给一般建议？

**答：** 因为趋势问题隐含“根据我的历史数据”这一前提。若数据不可用，模型给出“你最近进步明显”属于编造。系统可以额外提供通用恢复建议，但必须明确与个人历史无关；当前链路优先返回数据不足提示，保证事实边界。

### Q8：RabbitMQ 重复投递时如何保证不会重复分析？

**答：** 消息携带 taskId，任务表是唯一真相。消费者在开始分析前执行 `markProcessingIfQueued`，只有首次成功将状态从 QUEUED 迁移到 PROCESSING 才继续。第二次或并发消息看到的状态已变化，会直接跳过。乐观锁用于处理同时读取 QUEUED 的竞争。

### Q9：为什么 SSE 断开后仍要轮询？

**答：** SSE 是优化而不是一致性保证。浏览器休眠、网络变动、负载均衡超时都会断流。轮询承担最终一致性兜底，而指数退避避免用户多时造成每秒高频读请求。终态下停止轮询，避免无效流量。

### Q10：Redis 缓存如何避免返回过期个人训练结论？

**答：** 对短时重复读取使用 TTL，问答缓存默认 180 秒；视频分析或训练数据发生变化时，正式生产系统还应补充事件驱动失效或版本号 key。当前缓存是加速层，不能替代数据一致性设计。涉及医疗/运动安全的关键结论也不应仅依赖缓存。

### Q11：全量重建和增量索引怎么选？

**答：** 小型知识库、单个知识文件、低频更新时，全量重建更可靠：实现简单，删除与修改不会留下旧 chunk。大型多文档库、高频更新或 embedding 成本明显时，增量 Hash 索引更优：保存文档 hash、chunk hash、版本和来源，比较变更后只 upsert 新 chunk、删除失效 chunk。当前项目只有 24 个 chunk，全量重建的工程风险低于增量同步复杂度。

### Q12：当前最重要的技术债是什么？

**答：** 第一，中文 BM25 的分词质量需要改进并用 qrels 验证；第二，Reranker API 成功率与 fallback 次数尚未独立埋点；第三，趋势类评测账号需要准备真实完成的视频记录；第四，知识文档目前通过容器路径导入，生产部署应通过 Dockerfile 或 volume 固化；第五，任务 smoke 测试需要在完整依赖环境中产出重复投递、SSE 送达和端到端耗时报告。

## 9. 演示与口述建议

1. 先展示上传视频、任务进度和报告页面，解释异步任务与状态机。
2. 再展示问答页面：动作纠正问题应返回知识引用；趋势问题在没有历史数据时应明确提示数据不足。
3. 展示 `/rag/status`：确认 `chunk_count > 0` 且 `has_bm25 = true`。
4. 说明三层评测：RAG 排序、Agent 规划、真实 API 回归分别验证不同问题。
5. 主动说明边界：当前指标不等同于“视频动作识别准确率 100%”或“LLM 回答准确率 100%”。

## 10. 常用命令

```powershell
# 查看 RAG 索引状态
Invoke-RestMethod http://127.0.0.1:8000/rag/status

# 重建 RAG 索引
Invoke-RestMethod http://127.0.0.1:8000/rag/reindex -Method POST

# Agent 离线规划评测
cd ai-service
E:\Python311\python.exe scripts\agent_eval_runner.py

# RAG 离线评测（需 pytrec_eval 已安装）
E:\Python311\python.exe scripts\eval_runner.py --top-k 10

# 查看容器状态
cd ..
docker compose ps
```

---

## 面试中的一句话总结

这是一个把视觉动作分析、异步任务治理和 Agentic RAG 结合起来的健身应用：我没有把 LLM 当成唯一决策者，而是通过工具编排、知识引用、任务幂等、结构化输出和分层评测，把“模型能回答”转化为“链路可追踪、失败可降级、指标可解释”的工程系统。
